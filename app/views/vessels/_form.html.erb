<%= form_with(model: vessel, local: true) do |form| %>
  <% if vessel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vessel.errors.count, "error") %> prohibited this vessel from being saved:</h2>

      <ul>
      <% vessel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :vessel_name %>
  </div>

  <div class="field">
    <%= form.label :vessel_id %>
    <%= form.text_field :vessel_id, id: :vessel_vessel_id %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :vessel_type, id: :vessel_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
